package appinfo

// Version is the user-facing application version shown in shipped surfaces
// such as the About dialog. Release builds may override this via ldflags.
var Version = "v1.0.0"

// Description is the short product description shown in the About dialog.
const Description = "A desktop-first, keyboard-first flashcard tool for learning keyboard shortcuts without dashboard clutter."
